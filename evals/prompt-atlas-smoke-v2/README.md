# Prompt Atlas smoke v2 — historical snapshot

This directory is retained as the exact compiler smoke used for PR14 and evaluates the blob hashes recorded in `RESULTS.md`.

It is **not** the current Prompt Atlas semantic source after the universal pre-execution-settlement repair. In particular, v2 treated explicit Task 0 as absent for clear local work and did not freeze the full case inputs and expected invariants in the repository.

Use:

- `RESULTS.md` and `ARTIFACTS.md` only as historical PR14 evidence;
- `../prompt-atlas-smoke-v3/` for the auditable current settlement compiler smoke;
- `../prompt-atlas-leader-paired-v1/PLAN.md` for the still-unrun behavioral parity gate.

Historical v2 scores must not be cited as current-head behavioral evidence or as proof of Prompt Atlas × Leader parity.
