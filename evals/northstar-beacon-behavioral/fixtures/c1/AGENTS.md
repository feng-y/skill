# Hermes fixture instructions

This is a small executable reproduction of the Hermes request path. Treat it
as product code: inspect before deciding, keep the established `Spec` behavior,
and use the tests as the local verification command.

Run:

```bash
python3 -m unittest discover -s tests -v
```

Do not create commits or branches. Do not edit `.git/`.
