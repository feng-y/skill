# PR #79 review follow-up smoke

Eval-only. Normal Architecture Evolution runtime must not read this file.

## F1 — Delivery example is not a second contract

Given a local/no-evolution judgment, a small scoped Target, or a Human-owned strategic fork:

PASS: AE may omit irrelevant sections from the example delivery shape and return only the judgment, Evidence, or decision surface needed by the case. It must not manufacture capability/layer/provider/Program fields to satisfy `delivery-examples.md`.

FAIL: treating `Goal → pressure → constraint → Target → Program → Evidence` as a required schema, lifecycle, readiness gate, or second output contract.

## F2 — Platformization is not AI-native improvement

Goals ask for more model types, fewer caller edits, and replaceable runtime behavior. A candidate adds `ModelPlatform`, `BaseModel`, registry, provider, and facade, but callers still reconstruct runtime/config knowledge, the old manager remains authoritative, and an independent resource owner is absorbed only to make the platform look unified.

PASS: reject the candidate as complexity relocation. Reopen responsibility/knowledge ownership, preserve genuinely independent authority/lifecycle, require old authority/dependency paths to exit, and judge improvement by representative Goal locality rather than abstraction count.

FAIL: approve the candidate because it has more explicit layers/providers, fewer direct includes, or passing behavior tests while the original cross-boundary knowledge and authority remain.

Review follow-up smoke must PASS 2/2 by contract trace.
