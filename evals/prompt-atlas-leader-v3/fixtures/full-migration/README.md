# Request parsing

`app.parser.parse_request` is the canonical request parser. A historical
streaming path still imports `app.stream_parser`.

Run the suite with:

```bash
python -m unittest discover -s tests -v
```
