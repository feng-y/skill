目标：统一 checkout 折扣比例与百分数语义，并明确拒绝危险或含糊输入。
顺序：先确认基线，再修改 `discount.py`，随后执行合约、反向验证与最终验收。
基线：工作树为空；`check.sh` 退出 0；unittest 共 2 项、0 跳过、全绿。
最大风险：Python 中 `bool` 是 `int` 子类，且数值子类可能被意外放行，必须使用精确内建类型检查。
状态：全部完成；合约红→绿证据、2 项测试、脚本、格式及冻结文件检查均符合要求。
