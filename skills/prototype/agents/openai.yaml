interface:
  display_name: "Prototype"
  short_description: "构建受托问题的原型与 draft，不负责组合"
  default_prompt: "使用 $prototype，按 SKILL.md 构建或修正受托问题的原型与 draft；任务可以小，也可以复杂。完成约定实做并返回产物、观察、边界与缺口。不要承接局部产物的选择、拼装、调用调度或整体 Intent 对齐；组合由 Northstar 或其他调用方完成，Verify 保留证明判断。"
