interface:
  display_name: "Northstar"
  short_description: "处理 Drafted Issue 仍无法局部关闭的困难 Intent"
  default_prompt: "使用 $northstar，仅处理当前 Drafted Issue / engineering request 中会改变 accepted outcome、binding boundary、Human commitment 或 material dependency 的困难 Intent；能直接执行的 Issue 不重编译，Goal / Taskbook / Graph 只在增加决策或交接价值时生成；已有 execution contract 与结果时独立判卷。"
