interface:
  display_name: "Northstar"
  short_description: "定准 Goal，编译任务，不设计 patch"
  default_prompt: "使用 $northstar。定准 Human 真正要完成的 Goal，再编译 fresh Executor 的最小充分 Taskbook；只调查会改变当前 judgment 的 reality，safe-start 后把 implementation How 和 verifier composition 留给 Executor。若上下文已经包含 authoritative Taskbook 与 Executor outcome/Evidence，则直接按同一 contract 与 current reality 核验完成，不重编任务、不接管执行。"
