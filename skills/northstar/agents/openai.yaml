interface:
  display_name: "Northstar"
  short_description: "收敛并保持完整工程 Intent"
  default_prompt: "使用 $northstar，遵循其 SKILL.md，维护与原始请求或 Human 已确认范围一致的完整 current Draft。按需取得 specialist 的局部结果，由 Northstar 取舍、连接并修订同一个 Draft；material blocker 不算 ready，readiness 不产生执行授权，已有 scoped authorization 不重复确认。后续反馈只重开受影响语义，普通实现细节自主推进。"
