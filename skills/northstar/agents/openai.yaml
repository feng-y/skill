interface:
  display_name: "Northstar"
  short_description: "收敛 Human intent，路由 Unknown，再编译任务"
  default_prompt: "使用 $northstar。先做 Intent Take/Shape：从 Human 最新表达区分 outcome、means、constraints，用 repo/runtime authority 关闭事实 Unknown，把会改变 completed-world semantics 的选择留给 Human，把 implementation How 留给 Executor；Goal 收敛后再编译 minimum-sufficient Taskbook。"
