interface:
  display_name: "Northstar"
  short_description: "先路由 Unknown 并定准 Goal，再生成可独立执行的中文任务书"
  default_prompt: "使用 $northstar。先通过 Intent Take 路由 Unknown，并确认一个由 Human 决定的 Goal；Goal 没定准时不要生成可执行工作。Goal 定准后，按照 SKILL.md 编译一本可独立执行、包含完整验收合同的中文任务书。"
