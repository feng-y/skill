# 执行图：只表达真实依赖

只有线性 Task 列表会掩盖真实依赖或并行关系时，才使用执行图。

- `depends on`：只有下游确实要消费上游结果，或上游是安全执行前提时才写；
- `may run in parallel`：没有依赖，也没有写入冲突时才写；
- `reverify at join`：并行可以继续，但后续修改可能让已有证据失效时写。

每个分支最终都要重新汇合，或者走到一个明确终止路由。省略传递依赖，也不要把单纯先后顺序伪装成依赖边。写入所有权和证据要求仍放在普通 Task 合同里。不要发明 Graph schema、固定 Agent 拓扑或多本任务书。
