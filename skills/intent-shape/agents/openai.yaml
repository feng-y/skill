interface:
  display_name: "Intent Shape"
  short_description: "把已理解的工程意图具体化到可提交"
  default_prompt: "使用 $intent-shape，作为 Northstar 的 model-invoked shaping specialist，把当前已理解但仍可能产生 materially different Target 的关键意图，用最低成本的 Core Path / Usage / Prototype 具体化；得到 correction / Evidence 后返回 Northstar，不拆执行任务。"
