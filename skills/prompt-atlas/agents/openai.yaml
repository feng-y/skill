interface:
  display_name: "Prompt Atlas"
  short_description: "先收敛意图与 Unknown，再生成可独立执行的任务书"
  default_prompt: "Use $prompt-atlas. Route Unknown explicitly; never emit executable work from unresolved intent; once intent is stable, compile one compact autonomous taskbook per SKILL.md."
