interface:
  display_name: "Prompt Atlas"
  short_description: "Settle Goal, then write for the Executor"
  default_prompt: "Use $prompt-atlas. Work as Research → Ask → Write → Deliver: settle Goal first; probe ordinary facts yourself, and require sufficient reality Evidence before a material reality claim becomes contract; when available route coupled Unknown/full-map work to $unknowns-first and long-lived structural judgment to $architecture-evolution. Do not bypass an open Human-owned choice with a default or provisional Taskbook; leave implementation How to the Executor. After a Human correction, recompute only affected judgment, reconcile current constraints, and fully re-deliver; fold specialist results into the same Goal/Taskbook and materialize every successful Taskbook outside repo/workspace. If the input is Executor results/Evidence, do not rewrite an implementation Taskbook; independently judge them against the current Taskbook and verifiable reality, and do not treat done/task/test completion as the Goal outcome."
