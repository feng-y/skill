interface:
  display_name: "Prompt Atlas"
  short_description: "Settle Human intent, route Unknowns, then compile the task"
  default_prompt: "Use $prompt-atlas. Start with Intent Take/Shape: separate outcome, means, and constraints from the Human's latest wording; close factual Unknowns through repo/runtime authority, keep completed-world choices with the Human, and leave implementation How to the Executor. Compile a minimum-sufficient Taskbook only after Goal converges."
