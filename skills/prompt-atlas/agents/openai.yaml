interface:
  display_name: "Prompt Atlas"
  short_description: "把需求编译为稳定意图与紧凑可执行任务书"
  default_prompt: "Use $prompt-atlas to recover stable intent, ask only at the three Human boundaries, then emit an exact blocker or a compact six-section taskbook with fixed Task 0, a minimal graph, bounded retry, progress/blocker state, and trusted Global Gate acceptance. Keep implementation How adaptable inside confirmed intent and boundaries."
