interface:
  display_name: "Prompt Atlas"
  short_description: "整合用户跨轮次零散 hint，澄清 intent 后在方案与 SPEC 前停下"
  default_prompt: "Use $prompt-atlas to gather my scattered hints and clarify the intent without proposing a solution or spec."
