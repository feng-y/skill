interface:
  display_name: "Prompt Atlas"
  short_description: "把零散目标编译为可直接运行的 Goal Loop Contract"
  default_prompt: "Use $prompt-atlas to compile this goal or my scattered hints into a consumer-ready executable goal loop, or surface only the unresolved gap that still prevents safe execution."
