interface:
  display_name: "Prompt Atlas"
  short_description: "先接收真实意图并补充关键上下文，再生成自然可验证的任务合同"
  default_prompt: "Use $prompt-atlas to intake my intent, enrich only decision-changing context, and produce one concise task contract."
