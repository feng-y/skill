interface:
  display_name: "Prompt Atlas"
  short_description: "收敛 Intent，并生成带分层验证的自主任务书"
  default_prompt: "Use $prompt-atlas. Reduce material Unknown with proportionate evidence; keep Goal, acceptance, confirmed-boundary, and authority choices Human-owned, and never emit executable work from unresolved intent. Once intent is stable, compile one Goal-shaped autonomous taskbook with grounded Tasks, cheapest sufficient Task proof, cost-aware Task-Group verification, and complete-Goal acceptance per SKILL.md."
