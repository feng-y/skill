interface:
  display_name: "Prompt Atlas"
  short_description: "先收敛意图与 Unknown，再生成可独立执行的任务书"
  default_prompt: "Use $prompt-atlas. Separate the requested outcome from proposed means, route Unknown explicitly, and do not emit executable work from a vague request or unresolved intent. Once one Human-owned Goal is stable, compile a compact autonomous taskbook with grounded facts, visible defaults, boundaries, fixed Task 0, ordered tasks, PROGRESS/BLOCKED continuity, stop rules, anti-false-green proof, and complete-Goal acceptance."
