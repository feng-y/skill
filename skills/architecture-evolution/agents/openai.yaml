interface:
  display_name: "Architecture Evolution"
  short_description: "从仓库事实判断目标架构与下一步改进"
  default_prompt: "使用 $architecture-evolution。依据仓库事实判断问题是局部还是架构，收敛目标架构，并给出最多 3 个让同类变化更局部且让旧结构退出的改进项。不要预定实现。收敛后将同一正文写入仓库或工作区外的交接文件；用户作实质修正后，重算受影响判断并完整重交付。"
