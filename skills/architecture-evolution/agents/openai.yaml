interface:
  display_name: "Architecture Evolution"
  short_description: "判断长期 Target 并收敛当前结构演进"
  default_prompt: "使用 $architecture-evolution，从当前 Northstar Intent、仓库现实与仍有效的 architecture authority 中先建立或复用 Target Architecture，再判断 Current → Target 的 structural gap、focused Evolution Program 与 real exits；不要从当前 module/patch 反推 Target，不设计 implementation How，behavior verification 交 $replay。"
