interface:
  display_name: "Architecture Evolution"
  short_description: "从 Target 收敛当前结构演进 Program"
  default_prompt: "使用 $architecture-evolution，依据当前 Goal、仓库现实与仍有效的 Target Architecture 判断是否值得结构演进，并收敛高杠杆 Program 与 real exits；Target 缺失或前提失效时 model-invoke $architecture-shape，不设计实现 How。"
